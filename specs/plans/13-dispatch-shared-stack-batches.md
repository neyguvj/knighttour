# План 13. Count-фаза: общий lazy-batched стек задач (адаптация class mode, стратегия 3 из трёх)

## Статус

**WIN** (замеры 2026-09-15): лучший свип **K=10000, B=16** даёт `cnt_ms` средний **−5.6%** против
предрегистрированного контроля дня ×2 (**−5.9%** против среднего всех трёх контролей; три прогона
90 700/90 566/90 514, разброс 0.1%), каждый повтор строго под
всеми прогонами контроля — порог «−5% и вне диапазона повторов» взят средним, не одиночным прогоном.
Гейты корректности побитово во всех 11 прогонах ветки; peak RSS в полосе контроля (стек K=10000 —
3.36 МБ из бюджета ≤3.4 МБ). Остальные точки сетки [1000..5000]×{16,64} — то же направление
(−4.5…−5.4%) без регрессов, но n=1 или с бо́льшим разбросом; B=64 не лучше B=16 (эффект B тоньше
шума). Рекомендуемые параметры мержа: **K=10000, B=16** (дефолт `dispatchStackCapacity` 5000→10000,
`dispatchClaimBatch` 16 — без изменений; правку дефолта делать при мерже вместе с ADR-019).
Ветка `dispatch/stack` (c6e6e07) — кандидат исследования «лучший из трёх»: сильнее плана 12
(−4.9%) ещё на ~1 п.п., план 11 (+14.5%) далеко позади. Числа — ниже.

Сравнительное исследование диспатча count-фазы (планы 11/12/13 — три стратегии, каждая своей
веткой/фичем против общего свежего контроля HEAD; по замерам лучшая вливается в main, остальные
помечаются Отклонены с числами).

### Замеры (2026-09-15, стенд: Apple M4 Max, 14 CPU, macOS 26.3.1, go1.25.4 darwin/arm64)

Точка: `make bench-size N=7 DEPTHS=22`, workers=14, каждый прогон отдельным процессом;
GOGC=40 понижает конвейер. База (`BASE_REF` = main 9ef1fdd, in-place `Each`) — ×2 до свипа
(предрегистрированный контроль) + один после свипа на дрейф: хвостовой контроль 96 966 против
первых двух 95 873–96 099 — дрейф ≤ +1%, сравнение честно. Ветка — свип
`BENCH_COUNT_K` ∈ {1000, 5000 (дефолт), 10000} × `BENCH_COUNT_B` ∈ {16 (дефолт), 64};
K=5000/B=16 и K=10000/B=16 по ×3, остальные ×1.

| прогон | cnt_ms | Δ к среднему контроля ×2 (95 986) | total wall | peakRSS_MB | hits/misses |
|---|---|---|---|---|---|
| контроль main run1 | 96 099 | — | 212.2 с | 10 397 | 364 804 607 / 1 772 741 575 |
| контроль main run2 | 95 873 | — | 211.8 с | 10 705 | идентично |
| контроль main run3 (после свипа) | 96 966 | — | 212.7 с | 10 722 | идентично |
| ветка K=1000 B=16 ×1 | 91 073 | −5.1% | 206.9 с | 10 552 | идентично |
| ветка K=1000 B=64 ×1 | 91 180 | −5.0% | 208.6 с | 10 695 | идентично |
| ветка K=5000 B=16 ×3 (дефолт) | 90 130 / 92 240 / 92 579 | ср. −4.5% | 205.1–210.4 с | 10 598–10 716 | идентично |
| ветка K=5000 B=64 ×1 | 90 786 | −5.4% | 206.1 с | 10 671 | идентично |
| ветка K=10000 B=16 ×3 | 90 700 / 90 566 / 90 514 | ср. **−5.6%** | 206.4–206.8 с | 10 191–10 373 | идентично |

- Контроль дня: средний предрегистрированной пары ×2 = 95 986; с хвостовым — 96 313
  (диапазон 95 873–96 966, ±0.57%). Тот же main в контроле плана 12 тем же днём —
  96 294–96 975: стенд согласован между сессиями.
- Вердикт по лучшей точке (K=10000, B=16): средний 90 593 → **−5.6%** против предрегистрированной
  пары контроля и **−5.9%** против среднего всех трёх контролей; против самого быстрого контроля
  дня — −5.5%; все три повтора ниже и линии гейта (0.95×min-контроля = 91 079), и минимального
  контроля. Разброс точки 0.1% (vs ±1.3% у K=5000/B=16) — при глубоком стеке планировщик устойчив.
- Свип: плато [1000..10000] со слабым трендом в пользу больших K (как в плане 12), эффект B между
  16 и 64 неразличим на фоне шума (B=64 синхронизируется реже, но батч грубее балансирует — взаимно
  компенсировались). Лучшая измеренная точка — **K=10000, B=16**.
- Механизм: гипотеза подтвердилась. Батч-претензия ×B снимает то, что осталось от горла планов
  11/12: у плана 12 плато ~−4.9% не зависело от глубины бинов — упиралось в head-of-line
  (полный свой бин держит продюсера под RLock шарда); общий стек + LIFO снимают блокировку
  полностью, плюс ещё ~1 п.п. сверх плана 12. Риски не материализовались: single-producer обход
  не тормозит (cnt_ms −5.6% при одном обходе-горутине — копия 24 Б × ~131.8M записей успевает за
  счётом на 90 с; genB/genA без изменений), LIFO без LPT — лучше, чем что давали бины/каналы; contention
  одного мьютекса при B=16 не виден (контраст с планом 11, где общий hchan стоил +14.5%).
- Гейты корректности во всех прогонах: итог 165 575 218 320 (сверка бенча PASS), hits/misses
  побитово идентичны контролю; peak RSS ветки 10 191–10 716 против контроля 10 397–10 722 —
  в одной полосе (стек K=10000: 24 Б × 10000 × 14 = 3.36 МБ ≤ бюджета 3.4 МБ); totalAllocMB
  без дельт; allocs +~10² на процесс (инициализация стека), на запись — ноль.

Вердикт бенчмаркера: **WIN** → мерж и ADR-019 за оркестратором; дефолты при мерже: K→10000, B=16.

**Пост-мерж (2026-09-15).** Диспатч влит в main (код — 0a680f5, свип-заметка — 4009a46); дефолт
K 5000→10000 поднят в рабочем дереве вместе с формулировкой спеки и едет в финальном коммите
фикса. Приёмочная строка «свип 6×6 / точка 5×5 без глубин вне разброса» **провалена**: против
контроля `c3feac8` (inline-Each) на 6×6 — **d1 ×2.4, d2 ×1.8, d3 ×1.4**. Механизм и фикс — в
секции «Фикс механики» ниже; статус: **fix-forward в main**, пересъёмка гейтов открыта, ADR-019
пишется по её итогам (числа фикса + K).

Логи: `bench-logs/plan13/` (gitignored). Не снято (вне запроса точки): LIFO/FIFO-контрастная
точка, низкие глубины 7×7 (риск «дешёвое f(task) / однопоточный обход» там остаётся
непроверенным). Свип 6×6 и точка 5×5 из «вне запроса» выпали пост-мержем и стали обязательными —
переснимаются гейтом фикса (см. «Фикс механики»).

## Фикс механики (fix-forward): адаптивная гранулярность Beff

### Механизм регресса (по коду `countTasks`)

Единица диспатча — фиксированный батч B=16 с обеих сторон. При `записей < B` локальный буфер
продюсера не заполняется ни разу за обход, вся таблица уходит одним хвостовым флешем *после*
его завершения, и первый же pop забирает `min(B, записей)` — весь task-cache достается первому
консьюмеру, остальные завершаются не начав (инлайн-Each балансировал по 128 шардам). Полоса
разложения (writesB; при d≤5 это в точности записи task-cache — gen B вырожден):

| доска/глубина | записи | активных консьюмеров при B=16 | Δ к контролю c3feac8 |
|---|---|---|---|
| 6×6 d1 | 6 | 1 | ×2.4 |
| 6×6 d2 | 20 | ≤2 | ×1.8 |
| 6×6 d3 | 73 | ≤5 | ×1.4 |

### Контракт фикса (выбран пользователем)

`Beff = min(B, max(1, записей/(консьюмеры·C)))`, считается один раз в начале count-фазы
(`ItemsCount()` известен до создания стека — API пакета `cache` не меняется); обе стороны —
флеш продюсера и снятие консьюмера — используют одно Beff. Границы: per-record при
`записях ≤ консьюмеры·C`, потолок B при `записях ≥ B·консьюмеры·C` (= 896 при workers=14) —
большие таблицы работают ровно как измеренный WIN. Контракт перенесён в `specs/counter.md`
§counting (без rationale, прецеденты 1c20a82/c6e6e07).

### Константа C = 4

Граница формулы по измеренным размерам таблиц (workers=14; целочисленное деление):

| записи | что это | Beff при C=4 | примечание |
|---|---|---|---|
| 6 / 20 / 73 | 6×6 d1–d3 (регресс) | 1 | per-record, параллелизм = записи |
| 228 / 653 | 6×6 d4/d5 | 4 / 11 | d5 — кандидат в скрытую регрессию (см. гейты) |
| ≥ 896 | 6×6 d6+ и весь рабочий диапазон 7×7 | 16 (=B) | контракт WIN не тронут |

Обоснование C=4:

- **Смысл C**: нижняя граница числа снятий на консьюмера в переходной зоне. 4 чанка на воркера —
  штатная оверсубскрипция для задач без оценки стоимости; это ровно механизм выигрыша плана
  (батч-претензия оставляет slack, хвост схлопывается). C=1 превращает зону в «по одному снятию
  на консьюмера» (d3: Beff=5 → ~15 снятий на весь стек при 14 консьюмерах) — статическая полоса,
  head-of-line плана 12, от победы над которым и взят выигрыш.
- **Sync-бюджет**: пока формула активна, снятий ≤ ~2·консьюмеры·C ≤ 112 на сторону при любом
  размере таблицы; захват = один мьютекс + копирование ≤ Beff×24 Б — единицы микросекунд
  сериализации, невидимы против минимального измеренного cnt (0.145 мс, 5×5 d12). C=8/16 —
  вдвое/вчетверо больше захватов ради двух средних точек; резерв проверяется ручкой
  `BENCH_COUNT_C` (bench-only, по образцу K/B, без CLI-флага), вне обязательного свипа фикса.
- **Консервативность**: формула читает `ItemsCount()`, а не writesB (аддитивный Set на d≥6
  ужимает таблицу) — меньше записей только мельчает батч, регрессии это добавить не может.

### Шаги и гейты пересъёмки (принятие фикса)

1. Спека до кода: `specs/counter.md` §counting — контракт Beff (сделано этим изменением).
2. Реализация в `counter.countTasks` + табличный тест формулы границ; exact-once-тесты должны
   покрыть и малую таблицу с активной формулой (записей < B, доставка per-record). `make check`.
3. **Свип 6×6 d18..d1** (`make bench-size N=6`) против свежего контроля `c3feac8` (worktree),
   чередование контроль/ветка ×2/×3 по методике ADR-013 — собственные полосы повторов
   обязательны, дневной дрейф на средних глубинах заметен: одинокий прогон ветки d8 = 119.8 мс
   выше верхней границы полосы ADR-013 [104.4..116.4] (~+3%), а d5 = 194.3 мс выше полосы
   [166.3..176.8] (+10%, n=1 — вердикт только за свежим контролем; Beff(C=4)=11 там — целевой
   ответ фикса на случай, если регрессия d5 реальна и гранулярна).
4. **Точка 5×5**: дефолтная глубина 6 + границы формулы d1 (per-record) и d12 (наибольший стол
   таблиц 5×5, writesB=181), ×3.
5. Гейт: **ни одной глубины вне пересечения собственных диапазонов повторов** (формулировка
   ADR-013); бит-гейты `toursExpected` во всех прогонах. 7×7 не переснимается: записи 131.8M ≫
   границы насыщения 896, Beff≡B — путь WIN-контракта не меняется (следует из формулы; точкой не
   проверяется).
6. Финальный коммит: реализация Beff + `BENCH_COUNT_C` + поднятие дефолта K 5000→10000 (уже в
   рабочем дереве) + спека; ADR-019 — после пересъёмки, с числами фикса и K, строка в индекс.

### Риски фикса

- Per-record на малых таблицах умножает захваты относительно B=16 ровно до ≤ 2·консьюмеры·C —
  см. sync-бюджет; на 5×5 (cnt < 1 мс) верхняя оценка — десятки мкс, гейт пункта 4 заметит
  несостыковку.
- Аномалия d5 (rec≈653) может оказаться не гранулярностью (n=1, дрейф дня) — тогда фикс на ней
  нейтрален (Beff 16→11) и её закрывает свежий контроль гейта.

## Мотивация и базовые числа

Текущий диспатч count-фазы — `Cache.Each` (ADR-013): горутина на шард под RLock, колбэк
выполняет count-DFS inline под залом; единица работы — шард целиком → последние шарды держат
воркеров поодиночке, параллелизм падает.

Механика-прародитель — class mode (ADR-010, удалён ADR-016): stage 1 дренировал шарды в
плоские срезы и резал их на задачи; stage 2 — **общий стек под мьютексом**, воркеры брали с
вершины батчами `claimBatch = 16`, пустой стек = всё разобрано (новых задач не рождалось,
сна не было). Дрейн был возможен, потому что записи изымались из map (второго экземпляра
памяти не возникало), а финальный DP читал только свои срезы.

База — HEAD с `Each` (in-place контроль, ×2 прогона до кода). Точка сравнения: **7×7 d22,
workers=14** (`make bench-size N=7 DEPTHS=22`), метрика — `cnt_ms/op`; ориентир ADR-013:
cnt ≈ 93–99 с, peak RSS (GOGC=40) 10 203 МБ.

## Идея / Гипотеза

Переносим планировщик class mode на reversal **без дрейна**: продюсеры обхода кэша складывают
копии записей пачками в общий bounded-стек (LIFO) под мьютексом, воркеры берут с вершины
батчами и считают count-DFS вне залов. Дешёвого прокси стоимости задачи в reversal нет
(в class mode им было число концов формы), поэтому **без LPT** — порядок складывания произвольный,
выигрыш даёт только динамическая батч-претензия: любой воркер берёт работу из общего стека,
хвост схлопывается. Ожидаем `cnt_ms` −5% и вне диапазона повторов контроля; относительно
плана 11 гипотеза — батч ×16 снижает синхронизационный оверхед на порядок при той же
балансировке.

## Корректность

- **Дрейн запрещён** (отличие от class mode): count-DFS читает живой task-cache через `Get`
  (мемо раннего стопа) — изъятие записей из map сломает подсчёт. Поэтому стек хранит *копии*
  (`cache.Entry`, 24 Б), а обход под RLock остаётся read-only; `Get` видит все шарды до конца
  фазы (контракт `specs/cache.md` сохраняется).
- Итог не зависит от порядка: общий `atomic.Uint64`, детерминизм `f(task)` (инвариант
  ADR-012/013). LPT снят — он был эвристикой порядка, не корректности.
- Ровно-один-раз: каждая запись кладётся один раз; стек только убывает (новых записей после
  барьера генерации нет) — пустой + барьер продюсеров = конец, как в `processShapes`.
- Блокировка продюсера на полном стеке выполняется под RLock шарда и безопасна: писателей нет,
  консьюмеры работают без залов и всегда освобождают место.
- Отмена ctx: продюсеры останавливаются (`Each` → `ctx.Err()`), флаг closed; воркеры дренируют
  стек без подсчёта (пропуск задач при `ctx.Err()`, как сейчас) — частичный итог без паники;
  чужая ошибка `Each` — panic (`checkEachError`).

## Дизайн

Пакет `cache` **не меняется** (`Each` переиспользуется как обход-продюсер). Всё в
`counter.countTasks`, форма — узнаваемая адаптация удалённых `groupShards`/`processShapes`
(см. ADR-010; код — в истории git до `895aa56`):

1. Общий стек: предвыделенный `[]cache.Entry` ёмкости `workers×K` (K ∈ {1000…10000} — ручка
   тюнинга) + мьютекс + condvar (полный/пустой/closed). Один индекс вершины; pop — срез
  `batch = min(B, размер)` за один захват мьютекса (B — второй параметр свипа, старт 16 как
   `claimBatch` class mode).
2. Продюсеры: колбэк `Each(ctx, 1, …)` накапливает записи в маленький локальный батч
   (стек-буфер продюсера, переиспользуемый) и выкладывает его одним захватом мьютекса; при
   полном стеке — wait на condvar (под RLock шарда — законно). Это снимает 131.8M одиночных
   lock/unlock до ~131.8M/B. Принято с одной обходной горутиной вместо `workers`: у колбэка
   фиксированная сигнатура и нет хука на горутину, поэтому приватный флеш-батч законно имеет
   только один продюсер (альтернативы — goid-хаки или правка пакета `cache`, обе вне плана);
   скорость копирования на порядки выше потребления счётом, параллельные продюсеры гипотезе
   не нужны.
3. Консьюмеры: `workers` горутин крутятся на pop-батчах; каждая запись батча — прежний
   `countOneTask` (count-DFS вне залов шарда). Выход: closed && стек пуст.
4. Мониторинг без изменений семантики (`AddTasks(ItemsCount())`, отчёты из консьюмеров).

Тюнинг на 7×7 d22: K ∈ {1000, 2000, 5000, 10000} × B ∈ {8, 16, 32}. LIFO-vs-FIFO — одна
дополнительная точка свипа (у class mode LIFO работал в связке с LPT; без него порядок может
потерять актуальность — проверяем замером, не догадкой).

## Аллокационный бюджет

| структура | формула | 7×7 d22, W=14 |
|---|---|---|
| общий стек `[]Entry` cap W·K | 24 Б × K × workers | 0.34 МБ (K=1000) … 3.4 МБ (K=10000) |
| локальный батч продюсера | 24 Б × B × активные продюсеры | ≤ 8.8 КБ |

Аллокаций на запись — ноль (копия в предвыделенный массив стека; pop — срез, не new).
Одна аллокация на фазу + condvar-структуры. Пик ≤3.4 МБ — в шуме против task-cache ~10 ГБ.
Сравнение механик: class mode жил «в ноль» только благодаря дрейну (map освобождался); здесь
bounded-копия — сознательная цена отказа от дрейна; полный снапшот 131.8M×24 Б ≈ 3.2 ГБ —
запрещён (ADR-012/013) и не предлагается.

## Этап 0 — измерение потенциала (опционально, до кода стратегии)

В контрольном прогоне HEAD опубликовать гистограмму времен шардов count-фазы (отладочный
лог под env теста): если разброс суммарной стоимости шардов <5% — хвоста нет и вся триада
11/12/13 упрётся в шум; фиксировать до начала реализаций.

## Шаги

1. Контроль HEAD ×2 + Этап 0 (гистограмма шардов) на 7×7 d22.
2. Спека до кода: `specs/counter.md` (§counting — общий bounded-стек, батч-претензия, колбэк
   вне залов, семантика отмены). `specs/cache.md` — без правок (дрейна нет, контракт цел).
3. Реализация в `counter/counter.go`; `cache/` не трогать.
4. Тесты (`-race`): ровно-один-раз при стеке ёмкости 1×B; дедлок-тест принудительным
   переполнением; отмена ctx → частичный итог; инвариант итого к воркерам на 5×5/6×6.
   `make check`.
5. Бенч: свип K×B на 7×7 d22 (×повторы), LIFO/FIFO-точка, полный свип 6×6, точка 5×5.
6. Вердикт по порогу → WIN: merge + ADR-019 + правка спеки; иначе Отклонён с числами здесь.

## Метрики успеха

| метрика | база (свежий контроль HEAD ×2) | цель / порог принятия |
|---|---|---|
| `cnt_ms` 7×7 d22, workers=14 | контроль дня (~93–99 с по ADR-013) | **−5% и вне диапазона повторов контроля** |
| итог 7×7 | 165 575 218 320 | побитово — жёстко |
| hits/misses counting | 364 804 607 / 1 772 741 575 | идентично — жёстко |
| peak RSS (GOGC=40) | 10 203 МБ | в шуме (+≤4 МБ ожидаемо) |
| allocs/op | без роста на запись | рост ≤ инициализации стека |
| свип 6×6, точка 5×5 | HEAD контроль | ни одной глубины вне разброса повторов |

## Риски

- **Один мьютекс на всех** (продюсеры + консьюмеры): при малом B contention съедает выигрыш →
  свип B; при провале — отклонение в пользу 11/12.
- LIFO без LPT может оказаться хуже FIFO (классический стек class mode опирался на порядок
  «дорогие первыми») → отдельная точка свипа, решение по замеру.
- Продюсер под RLock ждёт на condvar полного стека — долго держит шард «открытым»; безопасно
  только при отсутствии писателей (caveat ADR-013; при будущем инкрементальном пополнении —
  пересматривать).
- Обход стал однопоточным: если узким местом окажется сам обход+копирование (низкие глубины,
  дешёвое `f(task)`), понадобится хук на горутину в `cache` или другая механика продюсера —
  решать замером, не догадкой.
- Возврат формы планировщика из удалённого class mode не должен затащить мёртвый код
  (`claimBatch`/`shardJob` как таковые) — переносится только механика стека (правило AGENTS.md).

## Затрагиваемые файлы / спеки

- код: `counter/counter.go`, `counter/counter_test.go`
- спеки (до кода): `specs/counter.md`; `specs/cache.md` — не меняется
- новый ADR при принятии: 019; смежные: ADR-010 (источник механики), ADR-012/013, план 09
