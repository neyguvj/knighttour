# Решения (ADRs)

Сюда вынесены **принятые решения с обоснованием и замерами**. Модульные спеки
ссылаются на ADR одной строкой и не пересказывают историю/бенчмарки.

Правило: одно решение = один файл `NNN-<slug>.md`. Формат — шаблон
`spec-writing` skill (Контекст → Решение → Последствия → Замеры → Ссылки).
Замеры фиксируются **навсегда**: каждое новое решение несёт свои числа до/после.

| № | Решение | Статус |
|---|---------|--------|
| 001 | Class mode: подсчёт через классы форм вместо мемо-таблицы и reversal | заменено (ADR-016) |
| 002 | Единый аддитивный Accumulator вместо memo-Cache; писатели через LocalSink | заменено (ADR-018) |
| 003 | Хранилище M — нативная Swiss-map; своя таблица и sync.Map отклонены | принято (пересмотр: план 01) |
| 004 | Хэш шарда только по State; чтение drain'ом без копирующего Snapshot | принято (шард-хэш); drain-чтение снято ADR-018 |
| 005 | Единый ключ `path.Path(state,end)`; `start` вне ключа; `ShapeKey` удалён | принято |
| 006 | Прунеры слиты в один stateless `Pruner`; глобальные проверки всегда вкл. | принято |
| 007 | L2-прунинг только в финальном DP и выключен по умолчанию | принято; механика снята (ADR-016) |
| 008 | Pre-DP shape-фильтр реализуемости включён по умолчанию (`L2ForcedChain`) | принято; механика снята (ADR-016) |
| 009 | Отклонённые эвристики: цветовой прунинг, Warnsdorff, endpoint-mismatch | отклонено |
| 010 | Counting: два stage'а per-shard, LPT-стек батчей; группировка на чтении | принято; механика снята (ADR-016) |
| 011 | Reversal возвращается вторым режимом (`-mode`) | принято (свод закрыт; default → reversal, перенос в коде — отдельный коммит) |
| 012 | Count-фаза reversal: ленивый per-shard обход вместо снапшота Entries() | принято (WIN; RSS ≤10.5 не берётся, GC-опция отложена) |
| 013 | Count-фаза reversal: прямой обход шардов под RLock (`Cache.Each`) без снимков | принято (WIN; GOGC=40 даёт 10.2 ГБ — цель ≤10.5 взята) |
| 014 | Reversal понижает GOGC до 40 на время работы (флаг `-gc-percent`) | принято (WIN; верификация пройдена) |
| 015 | 8×8: class-mode измерения сняты, механика — за reversal; вне A/B-свода | принято |
| 016 | Удаление class mode: reversal — единственный конвейер (пересъёмка дефолтов глубины) | принято; критерий таблицы глубин заменён (ADR-017) |
| 017 | Дефолты глубины по минимальному времени: {5:6, 6:14, 7:22, 8:14}; RSS-правило снято | принято |
| 018 | Одна таблица `cache.Cache`: `Accumulator` и `LocalSink` удалены, gen A пишет `Set` | принято (заменяет 002) |
| 019 | Диспатч count-фазы: общий bounded-LIFO стек + адаптивная батч-претензия Beff; K=10000 B=16 | принято (WIN; гейты 6×6/5×5 PASS) |
| 020 | Удаление env-хендлов бенч-харнесса: дефолты фиксированы, гейты/точка — `-bench` фильтры Makefile | принято (замеры n/a; заменяет 015 в части cap) |
| 021 | Feature = ветка вместо feature-flag: переключатель фичи в git, откат = не мержить | принято (процессное; замеры n/a) |
| 022 | Жёсткие гейты вместо текстовых инструкций: permission-конфиг + plugin-хуки | принято (процессное; замеры n/a) |
| 023 | Гейт G7 «make check перед git commit»: пассивный зелёный штамп вместо инлайн-прогона | принято (реализовано на ветке `02-gates-plugin-core`; замеры n/a) |
| 024 | Сессия оркестратора в main: контракт WORKTREE вместо перезапуска; гейт G8 — main read-only при активном worktree | принято (пересматривает 021 «Сессия»; замеры n/a) |
| 025 | Гейт G9 «тяжёлые бенчи только под flock»: stateless-проверка инвокации вместо текстовой дисциплины | принято (замеры n/a) |
| 026 | Относительные паттерны `../kt-*` в правах spec/benchmarker: edit-subject относителен, тильда не матчится | принято (исправляет G3/G4 из 024; замеры n/a) |
| 027 | Гейты гигиены G10/G11: новый `tools/*.py` требует карточку+индекс, логи/бинарники вне `work/` блокируются stateless-хуками | принято (замеры n/a) |
| 028 | Гейт G12 «спеки до кода»: правка `<pkg>/*.go` без изменённой спеки блокируется по git-состоянию, обход — сессия после `/quick` (хук `command.execute.before`) | принято (замеры n/a) |
| 029 | Гейт G7 v2: штамп — множество контентных отпечатков (рабочая копия+индекс), сверяемых в момент коммита; авто-commit субкоманды (`merge` без `--no-commit`, cherry-pick/revert/rebase/am) под матчером | проект (ветка `08-g7-stamp-v2`; стоимость механизма замерена) |
