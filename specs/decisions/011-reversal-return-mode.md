# ADR-011. Возврат reversal как второго режима подсчёта (флаг `-mode`)

## Статус

Принято (реализация по плану 06). A/B-замеры сняты 2026-09-12 (см. «Замеры»); перенос
default `-mode` заблокирован критерием RSS до плана 08 / ADR-012.

Дополнено 2026-09-13: остаток A/B-свода 7×7 снят (нижний подраздел «Замеры»), свод шага 6
плана 06 закрыт; RSS-блокировка default снята ADR-014. По итогам свода принято решение шага 7
(2026-09-13, пользователь): **глобальный default `-mode` → `reversal`**; перенос дефолта в коде —
отдельный коммит, судьба class mode (удаление) — отдельное будущее решение.

## Контекст

На стенде 7×7 d22 class mode проигрывает эталонному reversal (git `0d142f4`) по обеим
осям: 596 с / 23.5 ГБ против 252 с / 9.6 ГБ при идентичном дереве генерации. Канонизация
целого дополнения не дедуплицирует почти ничего, но уничтожает кросс-задачное
переиспользование подзадач общего task-cache (17.27G prune-событий финального DP против
1.73G). Планы 02/03B/04 подтвердили: время живёт в deep-классе, где структурные проверки
и хвостовое мемо бесполезны.

## Решение

Reversal портируется на текущую обвязку вторым режимом за флагом `-mode class|reversal`
(дефолт — `class` до вердикта A/B-свода). Двухфазная генерация и мониторинг общие;
отличается финал: gen B пишет сырые канонические префиксы в возвращённый `cache.Cache`
(task-cache), count-фаза идёт DFS'ом со стопом на уровне `totalCells − d` и ответом
`Σ Get(canon(U,u))/orbitSize`.

Осознанные отступления от эталона (`0d142f4`):
- `cache.Cache` **не знает о симметриях** (эталон держал `*symmetry.Symmetry` и канонизировал
  в Get/Set); единый `Get` вместо пары `Get/GetCanonical`, писатель канонизует сам;
- веса `uint64` вместо `int`; шарды 128 с хэшем по State (общий хелпер с Accumulator)
  вместо 64 по state+end;
- oracle-слой не переносится (эталонный прогон снят при `-oracle-depth 0`);
- генерация пишет в task-cache прямым `Set` (верный порт; LocalSink — только если профиль
  покажет contention на локах шардов);
- count-dispatch — чанк-воркеры с атомарным индексом по `Entries()` вместо per-shard `Each`.

## Последствия

- До этапа 7 плана живут два режима; оба обязаны переживать `make check`.
- Task-cache не дренируется по шардам и живёт до конца count-фазы — на низких глубинах
  дороже M; точка OOM — результат A/B, не баг.
- Статистика hits/misses зависит от порядка воркеров; тесты фиксируют только итог.

## Замеры

### 2026-09-12, A/B-свод (benchmarker). Стенд: Apple M4 Max, 14 CPU, 36 ГБ, Go 1.25.4 darwin/arm64

Флаги: `make bench-size N=… [DEPTHS=…]` c `BENCH_MODE=class|reversal`, `-benchtime=1x`
(каждая точка отдельным процессом; peakRSS — per-process); эталон — worktree `0d142f4`,
`go run`-бинарь + `/usr/bin/time -l`. Порт = working tree поверх `9800c64`.

**Калибровка эталона.** `0d142f4`, 7×7 d22, oracle-depth 0: **247.4 с / 9.55 ГБ** против
зафиксированных в ADR-001 252 с / 9.60 ГБ (−1.8% — стенд воспроизводит эталон; выводы ниже
стенд-в-стенд). Итог 165 575 218 320, writesB 1 397 913 888.

**P0, корректность (HEAD, `-mode reversal`, `go run`):** 5×5 → 1728, 6×6 → 6 637 920
(обе <1 с). Плюс гейт `toursExpected` в бенче: обе трубы все глубины 6×6 (×3 повтора) и
7×7 d18/d22 — расхождений 0.

**7×7, depth 22** (главная метрика плана; время — ns/op прогона, RSS — peakRSS_MB/op):

| метрика | class (HEAD) | reversal-порт r1 | reversal-порт r2 | эталон git `0d142f4` |
|---|---|---|---|---|
| время | 569.2 с | **215.1 с** | 213.0 с | 247.4 с (записано: 252) |
| peak RSS | 21 311 МБ | 18 865 МБ | 21 363 МБ | **9 550 МБ** |
| gen B / counting | 214.1 / 355.0 с | 118.7 / 96.4 с | — | 140.8 / 106.1 с |
| writesB / записей кэша | 3 591 773 354 / 284.4M классов | 1 397 913 888 / 131.8M задач | — | 1 397 913 888 / 131.8M |
| hits/misses counting | — (class) | 364 804 607 / 1 772 741 575 | — | 364 804 607 / 1 772 741 575 |
| итог | ✓ точный | ✓ точный | ✓ | ✓ |

**7×7, depth 18:** reversal-порт — 464.3 с / **4 193 МБ**, hits/misses 1.94G/9.29G, итог точный.

**6×6, полный свип d18…d1, 3 повтора на точку, медиана** (время — прогон; Δ% — reversal против
class; «шум» — |Δ| не превышает разброс повторов):

| d | class, мс | reversal, мс | Δ% | вердикт | class RSS, МБ | rev RSS, МБ |
|---|---|---|---|---|---|---|
| 18 | 124.4 | 220.1 | +77 | class | 43 | 35 |
| 17 | 97.4 | 177.1 | +82 | class (у rev разброс ±84) | 44 | 36 |
| 16 | 89.8 | 155.5 | +73 | class | 45 | 37 |
| 15 | 81.1 | 104.2 | +29 | class (шумный rev) | 45 | 36 |
| 14 | 79.6 | 66.7 | **−16** | **reversal** | 47 | 36 |
| 13 | 73.0 | 65.2 | **−11** | **reversal** | 47 | 36 |
| 12 | 79.2 | 68.2 | **−14** | **reversal** | 54 | 36 |
| 11 | 71.8 | 76.9 | +7 | class | 62 | 36 |
| 10 | 81.2 | 82.7 | +2 | шум | 89 | 36 |
| 9 | 76.1 | 93.5 | +23 | class | 105 | 36 |
| 8 | 98.9 | 103.2 | +4 | шум | 145 | 37 |
| 7 | 89.5 | 116.5 | +30 | class | 192 | 37 |
| 6 | 118.5 | 144.8 | +22 | class | 311 | 37 |
| 5 | 140.0 | 142.7 | +2 | шум | 356 | 37 |
| 4 | 200.9 | 231.1 | +15 | class | 554 | 37 |
| 3 | 215.9 | 245.0 | +14 | class | 554 | 37 |
| 2 | 336.0 | 386.7 | +15 | class | 680 | 37 |
| 1 | 291.3 | 741.5 | +155 | class | 680 | 37 |

**Вердикт.** WIN против базы (`9800c64`, class-only): на целевой 7×7 d22 reversal-порт быстрее
class в **2.65×** (215 с против 569 с) при RSS на 11% ниже и побитово точном итоге; быстрее и
эталонного git (−13…−14% при повторах ±1%). Критерий плана «RSS ≤10.5 ГБ» **не выполнен**:
пик порта 18.9–21.4 ГБ — ×2 к эталону стенд-в-стенд; вероятные причины (не исправлять задним
числом, проверять отдельной задачей): копия `Entries()` (~3.2 ГБ при 131.8M записей) при живом
task-cache + GC-headroom (totalAlloc 32.8 ГБ против неизмеренного у эталона). До решения по RSS
перенос default `-mode` (шаг 7 плана) не проводить.

**Точка перегиба class↔reversal.** На 6×6 reversal дешевле только в окне d12–d14 (−11…−16%);
вне его быстрее class, а на d≤3 class платит памятью (680 МБ против плоских 37 МБ). На 7×7
reversal монотонно дешевеет с ростом d до 22 (d18: 464 с / 4.2 ГБ → d22: 215 с / ~19–21 ГБ —
экономия времени покупается пиком).

**Pending (бюджет свода исчерпан):** class 7×7 d18/d14, reversal 7×7 d14 (оценочно часы и
риск своппинга по кривой task-cache); повторный эталон — не требуется (воспроизведён).
*(Закрыто прогоном 2026-09-13 ниже: оценки оказались пессимистичны в разы, риск не проявился.)*

### 2026-09-13, закрытие остатка свода 7×7 (benchmarker). Стенд: Apple M4 Max, 14 CPU, 36 ГБ, macOS 26.3.1, Go 1.25.4 darwin/arm64

HEAD `76ca1c6` — база и замер одно и то же дерево: это не A/B ревизий, а дозакрытые
**абсолютные точки** свода (Pending выше). Метод: `BENCH_MODE=class|reversal make bench-size
N=7 DEPTHS=<d>`, каждая точка отдельным процессом (`-benchtime=1x`); точки строго
последовательны на праздном стенде, своп за время всех прогонов не использовался. Reversal —
на продефолтном `-gc-percent 40` (ADR-014, понижает сам конвейер), class GC не трогает.
Внешний `GOGC` не выставлен. Гейт `toursExpected`: **все прогоны точны** (165 575 218 320).

**Новые точки** (время — ns/op прогона; RSS — peakRSS_MB/op; классы/writesB/hits-misses —
/op-счётчики FakeMonitor):

| точка | прогон | время, с | peak RSS, МБ | totalAlloc, МБ | gen B / cnt, с | прочее |
|---|---|---|---|---|---|---|
| class d18 | r1 | 694.6 | 9 106 | 341 839 | 26.7 / 667.8 | classes 102.9M, writesB 515.2M, zeros 13.3M |
| class d18 | r2 | 691.9 | 9 133 | 341 855 | 25.7 / 666.2 | — (шум: −0.4% / +0.3%) |
| class d14 | r1 | 1046.0 | 7 397 | 3 870 479 | 1.87 / 1044.1 | classes 15.2M, writesB 38.2M |
| class d14 | r2 | 1040.9 | 6 660 | 3 869 921 | 1.86 / 1039.0 | шум времени −0.5%; RSS ±5% (high-water дрожание) |
| reversal d14 | r1 | 1272.0 | 324 | 1 048 | 0.84 / 1271.1 | hits/misses 5 712 825 408 / 19 886 320 298 |
| reversal d14 | r2 | 1292.4 | 318 | 1 048 | 0.80 / 1291.6 | шум +1.6%; hits/misses побитово те же |
| reversal d18 (HEAD) | — | 492.6 | 2 583 | 8 344 | 12.8 / 479.8 | hits/misses побитово = прогону эпохи ADR-011 (1 938 320 248 / 9 289 955 077) |

Сверка reversal d18 с точкой эпохи ADR-011 (464.3 с / 4 193 МБ, диспатч `Entries()`,
GOGC=100): на HEAD время +6.1%, RSS −38.4%. Единичные прогоны на каждой руке; история
регресса времени уже прожита ADR-012/013 (на d22 — в шуме), здесь фиксируется как
наблюдение: на кэше меньшего размера гранулярность per-shard диспатча заметнее — тот же
эффект, что 6×6 d12 (+3.7%) в ADR-012. Оценки «часы и риск своппинга» не подтвердились:
18–21 мин на точку, пик 324 МБ у d14.

**Итоговая таблица свода 7×7 (все снятые точки обоих режимов).** Δ% — reversal против
class по времени; сноски — код/конфигурация источника числа:

| d | class, время | class, peak RSS | reversal, время | reversal, peak RSS (GOGC 40) | Δ% время | Δ% RSS |
|---|---|---|---|---|---|---|
| 22 | 569.2 с ¹ | 21 311 МБ ¹ | 211.8 с ² | 10 423 МБ ² | **−63%** | −51% |
| 18 | **693.2 с** (медиана ×2) | **9 120 МБ** (×2) | 492.6 с *(нов., HEAD)* | **2 583 МБ** *(нов., HEAD)* | **−29%** | −72% |
| 14 | **1 043.4 с** (медиана ×2) | **≈7 030 МБ** (×2, ±5%) | **1 282.2 с** (медиана ×2) | **≈321 МБ** (×2) | **+23%** | −95% |

¹ — ADR-011 (2026-09-12); class-путь кода с тех пор не менялся (ADR-012/013/014 — только
reversal). ² — верификация ADR-014 (n=6, медиана), тот же reversal-код, что HEAD.
totalAlloc по своду: class 342 ГБ (d18) / 3.87 ТБ (d14) против reversal 8.3 ГБ (d18) /
1.05 ГБ (d14) — class на низких глубинах прогоняет терабайты аллокационного шума, reversal
плоский по всем точкам.

**Выводы для шага 7 (без решения о default — оно вне полномочий замера).**

1. Память: на всём измеренном окне [14, 22] reversal дешевле class по пику на каждой глубине
   (−51…−95%); пик reversal растёт с глубиной (task-cache), пик class — тоже, но стартово
   выше на порядок и с терабайтным churn.
2. Время: на 7×7 crossover между d14 и d18 — при d ≥ 18 reversal быстрее (−29…−63%),
   на d14 медленнее (+23%). Тренд ADR-011 «reversal монотонно дешевеет к d22» подтверждён:
   1 282 → 493 → 212 с. Prod-дефолт глубины семёрки — 20, т.е. в зоне выигрыша reversal;
   точка d20 не мерялась (между d18 и d22 обе руки монотонны).
3. Шум: class ±0.5% время / ±5% RSS, reversal ±1.6% время, hits/misses побитово детерминированы
   содержимым (совпали даже с прогоном эпохи `Entries()` на d18). Все выводы выше — вне шума;
   Δ(d14, +23%) и Δ(d18, −29%) многократно превышают разброс повторов.

**Вердикт: WIN относительно ожиданий ADR-011** — все три Pending-точки сняты с повторами,
гейты точны, тренды подтверждены, заявленный риск (часы/своппинг) не проявился; новых
аномалий, кроме зафиксированного +6.1% на d18 против до-диспатчевой руки, нет.

## Ссылки

Код: `cache/cache.go`, `searcher/searcher.go`, `counter/counter.go`, `main.go`.
Спеки: cache, searcher, counter, types, monitoring, main. Планы: 06 (гипотеза, дизайн,
метрики успеха). Смежные ADR: 001 (исходный отказ от reversal), 002, 003, 004.
