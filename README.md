# Knight Tour — Поиск всех открытых маршрутов коня

![Test Status](https://github.com/neyguvj/knighttour/actions/workflows/test.yml/badge.svg)

Реализация алгоритма подсчёта всех открытых маршрутов коня (knight's tours) на досках от 5×5 до 8×8 с использованием параллельных вычислений и продвинутых оптимизаций.

**Открытый маршрут** — это последовательность ходов коня, при которой он посещает каждую клетку доски ровно один раз (не возвращаясь в начальную клетку).

## Quick Start

```bash
# Быстрый запуск на доске 5×5 (по умолчанию)
go run main.go

# Запуск на доске 6×6 с параллелизмом
go run main.go -size 6 -workers 14

# С более глубоким предварительным разбиением подзадач
go run main.go -size 6 -workers 14 -precompute-depth 12
```

## Параметры командной строки

| Флаг | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `-size` | `int` | `5` | Размер доски (от 5 до 8) |
| `-workers` | `int` | `1` | Количество параллельных воркеров (1 = последовательный режим) |
| `-precompute-depth` | `int` | `1` | Глубина предварительного разбиения подзадач (от 1 до size²/2) |

## Оптимизации

### 1. Битовые маски (uint64)
Состояние посещённых клеток — значение типа `State uint64`, один бит на клетку:
- Компактное хранение (до 64 клеток доски 8×8), копирование состояния — O(1)
- Все битовые операции инкапсулированы в пакете `state`: `Visit`, `Unvisit`, `Invert`, `Intersect`, `Union`
- Итератор `AllVisited() iter.Seq[int]` (range-over-func) скрывает перебор установленных битов от вызывающего кода
- Само состояние служит идеальным хэшем для кэша

### 2. Предвычисленный граф
Пакет `graph` при старте один раз строит для каждой клетки:
- Список соседей `[][]int` и побитовую маску соседей `[]State`
- Проверка смежности сводится к `mask & unvisited != 0` за O(1)

Соседи хранятся в фиксированном порядке возможных ходов коня — сортировка по Вонсдорфу сознательно не используется (см. «Измеренные и отвергнутые оптимизации»).

### 3. Симметрия доски (группа D₄)
Доска обладает 8 симметриями: 4 поворота и 4 отражения.
- `NewSymmetry(size)` один раз предрассчитывает LUT перестановок `[8][64]uint8`, канонические позиции, размеры орбит и канонические группы
- Поиск ведётся только из **канонических** стартовых позиций; результат умножается на размер орбиты (×8 для общих клеток, ×4 для углов/диагоналей, ×1 для центра нечётных досок)
- `Canonicalize(state, end)` приводит пару к лексикографически минимальному виду среди 8 трансформаций — нормализация ключа кэша, объединяющая симметричные поддеревья (в том числе префиксы из **разных** стартовых орбит)
- Горячий путь работает через чистые таблицы подстановки — без замыканий, делений и взятия остатка; `transformState` перебирает только установленные биты маски, а не всю доску

### 4. Пропуск стартов по чётности (нечётные доски)
Граф ходов коня на нечётной доске двудольный с дисбалансом цветов (например, 13 против 12 для 5×5), поэтому открытый тур обязан начинаться на клетке «большинства». `Graph.SholdSkip()` отсекает все старты с нечётным индексом ещё на этапе генерации подзадач.

### 5. Прунинг: dead-end + связность + эвристика концов (AdvancedPruner)
Двухуровневая проверка `ShouldPruneAfterVisit(last, unvisited)` — всегда включена:
- **Локальный dead-end** за **O(deg(last))** вместо полного скана доски: по инварианту родитель уже прошёл проверку, и посетить новую изолированную клетку мог только последний визит; маски соседей не копируются — читается предвычисленная маска графа за O(1)
- **Глобальные проверки** (только при `|unvisited| > 1`, на битовых масках):
  - у `last` нет непосещённых соседей → ветвь мертва;
  - **связность `G[unvisited]`** — оставшийся путь живёт только в непосещённых клетках, они обязаны образовывать одну компоненту: bitset flood-fill от младшего бита (расширение фронта ИЛИ-ем масок соседей), заодно считается степень каждой клетки
  - **эвристика концов** — гамильтов путь имеет ровно 2 конца; ≥3 вершин степени 1 → prune, ровно 2 и ни одна не соседна `last` → prune
- Эквивалентность наивной реализации и корректность (ни одна проверка не отсекает полный маршрут) покрыты вероятностными тестами

Замеры полного подсчёта (M4 Max): 5×5 — **3.20 мс → ~0.7 мс**, 6×6 — **16.7 с → ~1.0 с** против прунера только с dead-end. Порог по остатку (K=20) ускорял 5×5 на ~8%, но на 6×6 проигрывал ~2× — выбрано «всегда».

### 6. Кэширование с шардингом и симметрией
Потокобезопасный кэш мемоизации (`cache/`):
- **64 шарда**, каждый под собственным `sync.RWMutex`
- Аллокационно-бесплатный мультипликативный хэш; номер шарда — старшие 6 бит
- Ключ — каноническая пара `(visited bitboard, end)` **без start**: число продолжений маршрута зависит только от посещённой маски и текущей клетки, поэтому симметричные состояния сливаются в одну запись, а префиксы из разных стартовых орбит схлопываются в одну подзадачу
- Value — агрегированный вес `Σ count·orbitSize`: умножение на орбиту зашивается **при генерации** (единственный корректный момент, т.к. ключ не содержит start), `Set` накапливает веса (`+=`)
- Итоговый вклад подзадачи: `completions(key) × weight` — на этапе подсчёта ни `start`, ни `GetOrbitSize` не нужны
- `Each(ctx, workers, f)` — параллельный обход всех шардов (питает вторую фазу подсчёта)

Полезен на досках ≤6×6 (ускорение 2–3×); на 7×7 и выше доля попаданий менее 1% из-за экспоненциального пространства состояний.

### 7. Разбиение на подзадачи (precompute-depth)
Двухфазный параллельный алгоритм:
1. **Генерация подзадач** (см. ниже — одно- или двухфазная): префиксы, достигшие заданной глубины, сохраняются в кэш с весом орбиты и дальше не раскручиваются (ветви отсекает тот же прунер)
2. **Подсчёт:** каждый элемент кэша обрабатывается воркером через `Cache.Each`; найденный результат умножается на вес записи (`Σ count·orbitSize`), агрегация — в общий `atomic.Uint64`. Подсчёт идёт через `CountPathsWithReversal` с досрочным завершением через реверс (см. следующий раздел)

Обе фазы выполняет **единый горячий DFS** `dfs` по битовым маскам без колбэков и аллокаций: различие сводится к базовому случаю (`depth = totalCells, cache = nil` для полного подсчёта; `depth = precomputeDepth, cache ≠ nil` для генерации префиксов).

Мелкие подзадачи лучше распределяются между воркерами, а симметричные префиксы схлопываются в одну запись с суммарным весом.

#### Одно- и двухфазная генерация подзадач

Канонических стартовых групп на досках 5–6 всего ~6, поэтому простого параллелизма «по старта» не хватает: при большом `precompute-depth` воркеры простаивают. Стратегия выбирается по глубине относительно константы `TwoPhaseBaseDepth = 5`:

- **Однофазная** (`depth ≤ 5`): DFS от каждой канонической стартовой группы, группы обрабатываются параллельно через `errgroup` с `SetLimit(workers)` — групп достаточно для загрузки воркеров
- **Двухфазная** (`depth > 5`):
  - **Фаза A:** заполнение промежуточного кэша на фиксированной глубине `TwoPhaseBaseDepth` (тот же однофазный путь)
  - **Фаза B:** каждая из тысяч промежуточных записей независимо докручивается `ExtendSubtask` до целевой глубины — параллелизм в тысячи раз выше, чем у стартовых групп

Корректность двухфазной генерации: граф и прунер D4-эквивариантны, поэтому продолжения всех симметричных копий префикса изоморфны — докрутка канонического представителя с агрегированным весом `W(entry)` воспроизводит **ровно то же содержимое кэша**, что и однофазная генерация (покрыто тестами).

### 8. Досрочное завершение через реверс (вторая фаза)

Префикс-кэш переиспользуется во второй фазе как **таблица обратных ответов** (meet-in-the-middle).

**Тождество реверса.** Для узла `(T, t)` с непосещёнными `U = full &^ T` число продолжений равно числу обращённых суффиксов — путей, покрывающих ровно `U` и входящих в соседа `t`:

```
f(T, t) = Σ_{u ∈ U, u ~ t} h(U, u),   h(U,u) = #(путей, покрывающих U и кончающихся в u)
```

Значение кэша `W(K)` — сумма по D4-орбите ключа (`W(K) = |fiber(K)| · h(U,u)`), поэтому для любой конкретной пары:

```
h(U, u) = W(canon(U, u)) / orbitSize   // деление всегда точное
```

- count-DFS останавливается на уровне `totalCells − precomputeDepth` и отвечает lookup'ом вместо дальнейшего спуска; включается только при `2d ≤ n²`, иначе — чистый полный поиск
- при `d = n²/2` каждая подзадача решается одним набором lookup'ов (чистый meet-in-the-middle)
- **безопасность прунинга:** нужный обращённый суффикс продолжается через `t` обратно по уже покрытому `T`, поэтому не мог быть отрезан `ShouldPruneAfterVisit` на генерации (прунинг D4-эквивариантен); на нечётных досках старты нужных путей — концы реальных туров, правильного цвета
- **стоимость:** одно сравнение `CountBits(st) == stopLevel` на узел; маска `U` трансформируется один раз на узел (`TransformStates`), канонизация пар с общей маской идёт по готовым образам (`CanonicalFromStates`) + один `GetCanonical`

В связке с двухфазной генерацией (см. предыдущий раздел) деградация на больших глубинах устранена: 6×6 при depth=18 — ~0.26 s → ~0.086 s, оптимум сместился на глубину ≈14–15 (~0.063 s).

### 9. Мониторинг
`RealMonitor` без блокировок (атомарные счётчики) перезаписывает одну строку прогресса раз в секунду — **по активной фазе** (`generation`, либо `gen A`/`gen B` при двухфазной генерации, затем `counting`):
```
[1.234s] Phase gen B | Tasks: 653/653 (100.0%) | Paths 0 | Writes 705311 | Pruned 2012131
```
`Cached paths` переименован в `Writes` — это число вызовов `cache.Set`, а не уникальных записей; добавлен счётчик `Pruned` (ветви, отсечённые прунером) — считается в обеих фазах. Финальный отчёт печатает строку на каждую фазу:
```
=== Final ===
Total time: 94ms
Phase gen A [205µs]: tasks 6/6 | paths 0 | writes 988 | pruned 292
Phase gen B [66ms]: tasks 653/653 | paths 0 | writes 705311 | pruned 2012131
Phase counting [28ms]: tasks 140373/140373 | paths 6637920 | writes 0 | pruned 0
Total paths: 6637920
```
Накладные расходы — менее 1%; `FakeMonitor` используется в тестах.

### Измеренные и отвергнутые оптимизации
- **Баланс цветов как критерий отсечения:** на 5×5/6×6 срабатывает 0 раз — инвариант выполняется автоматически вдоль любой легальной DFS-ветви, а для нечётных досок старты фильтрует `SholdSkip`
- **Сортировка кандидатов по Вонсдорфу:** количество узлов дерева поиска не меняется вовсе (прунер локален и не зависит от порядка), при этом время растёт на 10–20% из-за сортировки в каждом узле

## Результаты тестирования

Замеры на Apple M4 Max (14 ядер). Итоговое значение — число открытых туров с учётом всех симметрий.

### Доска 5×5 (1728 открытых туров), workers=14

Каждая глубина — строки по фазам (`generation` при depth ≤ 5; `gen A` + `gen B` выше). «Задач»/«Paths»/«Cache writes»/«Pruned» — счётчики монитора соответствующей фазы.

| Глубина | Время | Фаза | Время фазы | Задач | Paths | Cache writes | Pruned |
|---------|-------|------|------------|-------|-------|--------------|--------|
| 1 | ~406 мкс | generation | ~9 µs | 6/6 | — | 4 | 0 |
|  |  | counting | ~386 µs | 4/4 | 1728 | — | 4476 |
| 2 | ~315 мкс | generation | ~38 µs | 6/6 | — | 18 | 0 |
|  |  | counting | ~263 µs | 6/6 | 1728 | — | 2154 |
| 3 | ~255 мкс | generation | ~41 µs | 6/6 | — | 42 | 12 |
|  |  | counting | ~202 µs | 15/15 | 1728 | — | 2148 |
| 4 | **~227 мкс** | generation | ~36 µs | 6/6 | — | 84 | 50 |
|  |  | counting | ~179 µs | 32/32 | 1728 | — | 2062 |
| 5 | ~233 мкс | generation | ~56 µs | 6/6 | — | 140 | 110 |
|  |  | counting | ~167 µs | 59/59 | 1728 | — | 2033 |
| 6 | ~278 мкс | gen A | ~59 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~71 µs | 59/59 | — | 81 | 105 |
|  |  | counting | ~136 µs | 73/73 | 1728 | — | 1787 |
| 7 | ~257 мкс | gen A | ~55 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~58 µs | 59/59 | — | 112 | 169 |
|  |  | counting | ~134 µs | 103/103 | 1728 | — | 1713 |
| 8 | ~263 мкс | gen A | ~63 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~57 µs | 59/59 | — | 128 | 362 |
|  |  | counting | ~131 µs | 104/104 | 1728 | — | 1274 |
| 9 | ~237 мкс | gen A | ~59 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~73 µs | 59/59 | — | 158 | 455 |
|  |  | counting | ~92 µs | 131/131 | 1728 | — | 1123 |
| 10 | ~280 мкс | gen A | ~61 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~77 µs | 59/59 | — | 158 | 707 |
|  |  | counting | ~130 µs | 117/117 | 1728 | — | 614 |
| 11 | ~285 мкс | gen A | ~57 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~83 µs | 59/59 | — | 191 | 789 |
|  |  | counting | ~132 µs | 142/142 | 1728 | — | 501 |
| 12 | ~234 мкс | gen A | ~61 µs | 6/6 | — | 140 | 110 |
|  |  | gen B | ~97 µs | 59/59 | — | 181 | 1105 |
|  |  | counting | ~63 µs | 118/118 | 1728 | — | 40 |

### Доска 6×6 (6 637 920 открытых туров), workers=14

| Глубина | Время | Фаза | Время фазы | Задач | Paths | Cache writes | Pruned |
|---------|-------|------|------------|-------|-------|--------------|--------|
| 1 | ~761 мс | generation | ~31 µs | 6/6 | — | 6 | 0 |
|  |  | counting | ~761 ms | 6/6 | 6637920 | — | 16491396 |
| 2 | ~385 мс | generation | ~35 µs | 6/6 | — | 27 | 0 |
|  |  | counting | ~385 ms | 20/20 | 6637920 | — | 11469126 |
| 3 | ~257 мс | generation | ~55 µs | 6/6 | — | 100 | 11 |
|  |  | counting | ~257 ms | 73/73 | 6637920 | — | 11469116 |
| 4 | ~191 мс | generation | ~67 µs | 6/6 | — | 341 | 49 |
|  |  | counting | ~191 ms | 228/228 | 6637920 | — | 10637446 |
| 5 | ~160 мс | generation | ~129 µs | 6/6 | — | 988 | 292 |
|  |  | counting | ~160 ms | 653/653 | 6637920 | — | 10449172 |
| 6 | ~140 мс | gen A | ~131 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~521 µs | 653/653 | — | 1807 | 562 |
|  |  | counting | ~139 ms | 1610/1610 | 6637920 | — | 9414149 |
| 7 | ~125 мс | gen A | ~134 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~787 µs | 653/653 | — | 4325 | 2308 |
|  |  | counting | ~125 ms | 3805/3805 | 6637920 | — | 9146535 |
| 8 | ~112 мс | gen A | ~186 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~1.1 ms | 653/653 | — | 9834 | 6779 |
|  |  | counting | ~110 ms | 7591/7591 | 6637920 | — | 7644668 |
| 9 | ~104 мс | gen A | ~153 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~1.8 ms | 653/653 | — | 19850 | 17533 |
|  |  | counting | ~102 ms | 14721/14721 | 6637920 | — | 7003627 |
| 10 | ~91 мс | gen A | ~155 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~3.1 ms | 653/653 | — | 37992 | 40183 |
|  |  | counting | ~87 ms | 24194/24194 | 6637920 | — | 5600922 |
| 11 | ~87 мс | gen A | ~169 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~6.6 ms | 653/653 | — | 65119 | 82830 |
|  |  | counting | ~80 ms | 39130/39130 | 6637920 | — | 4879530 |
| 12 | ~75 мс | gen A | ~142 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~9.4 ms | 653/653 | — | 108214 | 157702 |
|  |  | counting | ~65 ms | 55013/55013 | 6637920 | — | 3630706 |
| 13 | ~71 мс | gen A | ~124 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~15.4 ms | 653/653 | — | 162621 | 277681 |
|  |  | counting | ~56 ms | 76614/76614 | 6637920 | — | 2899857 |
| 14 | ~65 мс | gen A | ~122 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~22.1 ms | 653/653 | — | 240830 | 459337 |
|  |  | counting | ~43 ms | 94565/94565 | 6637920 | — | 1911885 |
| 15 | **~64 мс** | gen A | ~193 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~30.9 ms | 653/653 | — | 326283 | 710708 |
|  |  | counting | ~33 ms | 115639/115639 | 6637920 | — | 1330180 |
| 16 | ~66 мс | gen A | ~145 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~41.2 ms | 653/653 | — | 446532 | 1051863 |
|  |  | counting | ~24 ms | 128168/128168 | 6637920 | — | 689279 |
| 17 | ~73 мс | gen A | ~126 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~52.3 ms | 653/653 | — | 548502 | 1498070 |
|  |  | counting | ~21 ms | 139548/139548 | 6637920 | — | 310954 |
| 18 | ~95 мс | gen A | ~142 µs | 6/6 | — | 988 | 292 |
|  |  | gen B | ~67.4 ms | 653/653 | — | 705311 | 2012131 |
|  |  | counting | ~27 ms | 140373/140373 | 6637920 | — | 0 |

Пояснения к колонкам:
- **Задач** фазы `counting` — число записей финального кэша (префиксов), т.е. старый столбец «Префиксов в кэше»; при depth > 5 генерация двухфазная, и задачи/записи фаз A и B видны отдельно.
- **Cache writes** — число вызовов `cache.Set` (один ключ может перезаписываться из разных префиксов), а не число уникальных записей.
- **Pruned** считается в обеих фазах; при depth = 18 (meet-in-the-middle, `2d = n²`) count-DFS отвечает lookup'ом сразу на stop level и не спускается — поэтому pruned = 0, вся работа прунера переехала в gen B.

На обеих досках время вышло на плато (5×5 — ~0.23 мс начиная с глубины 4; 6×6 — оптимум ~0.064 с на глубине 14–15). Двухфазная генерация убрала деградацию на больших глубинах: 6×6 при depth=18 время gen B растёт линейно (~67 мс), а фаза подсчёта, наоборот, сжимается до ~27 мс.

### Доска 7×7 (165 575 218 320 открытых туров)

| Конфигурация | Время | Задач | Префиксов в кэше |
|--------------|-------|-------|------------------|
| workers=14, depth=19 | **~17m 13s** | 62 925 975 | 713 252 162 |

> Метрики сняты до разделения мониторинга по фазам (счётчики суммировали генерацию и подсчёт).

> Комбинация глубокого precompute-depth и параллелизма даёт почти линейное ускорение: подзадач много, и они мелкие — воркеры загружены равномерно. Точные значения времени зависят от конфигурации системы.

## Бенчмарки

```bash
# Запуск бенчмарков (counter/, -benchtime=10x)
make bench

# или напрямую
go test -v -run=^$ -bench=. -benchmem ./counter/

# Пример вывода (Apple M4 Max, darwin/arm64)
BenchmarkCountAllToursParallel/size5/depth1-14  	      10	    596100 ns/op	   19830 B/op	     237 allocs/op
...
BenchmarkCountAllToursParallel/size5/depth8-14  	      10	    187583 ns/op	   21912 B/op	     266 allocs/op
...
BenchmarkCountAllToursParallel/size5/depth12-14 	      10	    212642 ns/op	   23499 B/op	     275 allocs/op
...
BenchmarkCountAllToursParallel/size6/depth1-14  	      10	 715384512 ns/op	   14356 B/op	     226 allocs/op
...
BenchmarkCountAllToursParallel/size6/depth12-14 	      10	  90832442 ns/op	 3911873 B/op	    1285 allocs/op
...
BenchmarkCountAllToursParallel/size6/depth18-14 	      10	 244030392 ns/op	13959907 B/op	    2141 allocs/op
```

**Измерения производительности на доске 5×5 (все воркеры):**
- **Время выполнения**: ~0.6 мс
- **Память**: 21,094 байт на операцию
- **Выделений**: 240 на операцию

## Проверка кода

Полная проверка одной командой:

```bash
make check    # fmt → vet → test -race → fix (автофиксы) → lint
```

Отдельные цели Makefile:

| Команда | Что выполняет |
|---------|---------------|
| `make build` | `go build ./...` |
| `make fmt` | `go fmt ./...` |
| `make vet` | `go vet ./...` |
| `make test` | `go test -race ./...` — тесты с проверкой гонок данных |
| `make fix` | `go fix ./...` + `golangci-lint run --fix ./...` — автофиксы современных идиом |
| `make lint` | `golangci-lint run ./...` — статический анализ |
| `make bench` | Бенчмарки пакета `counter/` |
| `make clean` | `go clean -testcache` |

